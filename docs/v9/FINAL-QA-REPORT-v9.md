# گزارش کنترل نهایی دستیار وکیل — نسخه 9.0.0

تاریخ کنترل: ۱۴۰۵/۰۷/۰۳ (2026-09-25)

## نتیجه اجرایی

نسخه Android با `versionCode=10` و `versionName=9.0.0` با موفقیت در حالت Release ساخته شد. آزمون خودکار رابط و محتوا ۱۶ از ۱۶ و آزمون امنیتی احراز/حذف اطلاعات ۷ از ۷ مورد را گذراند. APK با طرح‌های امضای v1، v2 و v3 معتبر و با `zipalign` سازگار است. همه ۹ فایل asset داخل APK با سورس نهایی یکسان‌اند.

این گزارش «کامل بودن حقوقی کل پایگاه رسمی» یا «عدم نمایش قطعی هشدار شهرت Play Protect برای نصب جانبی» را تأیید نمی‌کند؛ محدودیت‌ها در بخش پایانی آمده‌اند.

## کنترل اول — ساختار، رابط و عملکرد

- کادر «خدمت ویژه» از صفحه اصلی حذف شده است.
- خدمات اصلی به‌صورت ردیف افقی چرخشی/قابل پیمایش و با سه کارت قابل مشاهده طراحی شده است.
- دسته‌بندی قوانین ۲۴ کارت سه‌ستونه دارد و دسته‌های بدون داده با شمارش صفر غیرفعال‌اند.
- قانون مدنی به ۱۳۹ بخش واقعی با عنوان و محدوده مواد تقسیم و به‌صورت accordion باز می‌شود.
- جست‌وجوی سراسری میان قوانین، مواد، آرای وحدت رویه و نظریات موجود، نتیجه مرتبط بازمی‌گرداند.
- دقیقاً ۱۰۰ چک‌لیست حقوقی، ۱۰۰ چک‌لیست کیفری و ۲۰۰ نمونه متن متناظر ساخته شده است.
- ۱۷ محاسبه‌گر در صفحه محاسبات قابل مشاهده است.
- رابط هوش مصنوعی برای OpenRouter، Groq، Hugging Face و حالت شورای چندارائه‌دهنده وجود دارد.
- کنترل مرورگری: `PASS 16/16` و بدون خطای JavaScript.

## کنترل دوم — محتوا و داده

- ۲۵ متن کامل قانون در بسته وجود دارد.
- ۳۰۵ رأی وحدت رویه دارای متن برای همه سال‌های ۱۳۷۰ تا ۱۴۰۵ در شاخص موجود است.
- شمارش نمونه آراء: ۱۳۷۰: ۲۲؛ ۱۴۰۰: ۱۰؛ ۱۴۰۳: ۱۵؛ ۱۴۰۴: ۱۶؛ ۱۴۰۵: ۳.
- ۲۳۲۳ نظریه/نمایه موجود است: ۱۴۰۰: ۱۲۱۲؛ ۱۴۰۳: ۲۲۷؛ ۱۴۰۴: ۶۲۳؛ ۱۴۰۵: ۲۶۱.
- عبارت «مشاهده منبع» زیر نمونه لوایح نمایش داده نمی‌شود.
- متن‌های حقوقی، آراء و نظریات بدون جعل رکورد برای پرکردن خلأها نگهداری شده‌اند.

## کنترل سوم — امنیت، ساخت و بسته

- Build نوع Release و `lintVitalRelease` با موفقیت انجام شد.
- بسته: `ir.dadban.app`؛ حداقل Android 6 (`minSdk 23`)؛ هدف Android API 37.
- امضا: یک امضاکننده RSA-4096؛ گواهی SHA-256 برابر `5FE185556BD1A07439F5AFAE93DF2898A1340D1B99A2E1EF7B9E6DCF7D37D2D4`.
- APK با v1/v2/v3 امضا و هم‌تراز است؛ AAB نیز امضای JAR معتبر دارد.
- مجوزها فقط شامل اینترنت، زیست‌سنجی، اعلان/لرزش، هشدار دقیق و بازیابی هشدار پس از راه‌اندازی است.
- WebView دسترسی فایل/محتوا، DOM storage، mixed content و debugging را می‌بندد.
- کلیدهای AI داخل APK جاسازی نشده‌اند؛ کلید شخصی کاربر رمزگذاری می‌شود و JavaScript نمی‌تواند مقدار آن را بخواند.
- مقصدهای AI سه endpoint ثابت HTTPS هستند؛ redirect دنبال نمی‌شود؛ ورودی مدل/متن محدود و خروجی برای DOM escape می‌شود.
- pool اجرای AI محدود و هنگام نابودی Activity متوقف می‌شود.
- آزمون احراز هویت/PIN و حذف امن داده: `PASS 7/7`.

## شناسه فایل‌های انتشار

- APK SHA-256: `DF2F6E24FF3F7DEFA93D881B0724E9A87D71D3DA73188D31826AB0CBF1359411`
- AAB SHA-256: `E1E0C1FFADB6D931544F99D5717A8E5EAF33B80A7F5D4FB308D7B5BA3650F978`

## محدودیت‌ها و موارد باز

1. نظریات مشورتی از ۱۳۷۰ تا امروز کامل نیستند؛ داده فعلی فقط سال ۱۴۰۰ و ۱۴۰۳ تا ۱۴۰۵ را دارد. درگاه رسمی مورد آزمون، دریافت ماشینی را در محیط ساخت با WAF رد کرد. هیچ داده‌ای برای پنهان‌کردن این خلأ جعل نشده است.
2. «کامل بودن مطلق» همه آرای وحدت رویه باید با فهرست مرجع رسمی مستقل تطبیق شود؛ آزمون فعلی وجود متن و پوشش همه سال‌های ۱۳۷۰ تا ۱۴۰۵ در مجموعه را ثابت می‌کند.
3. اتصال واقعی AI بدون کلیدهای شخصی کاربر اجرا نشده است. ساخت native، کنترل endpoint، رابط رضایت و خطا بررسی شده‌اند، اما سهمیه و دسترس‌پذیری سرویس ثالث قابل تضمین نیست.
4. APK امضای معتبر دارد، ولی SourceStamp و شهرت Google Play ندارد. هشدار «برنامه ناشناخته/مضر» نصب جانبی ممکن است تا انتشار AAB در Google Play و عبور از بازبینی/اعتبارسنجی ادامه یابد. این هشدار با دورزدن حفاظ‌های گوشی رفع نشده است.
5. ارتقا از نسخه ۰٫۷ با همین امضا سازگار است. نسخه‌های قدیمی ۰٫۵/۰٫۶ با امضای دیباگ نیازمند خروجی‌گرفتن از داده و نصب پاک هستند.
